# Prism Chronicle — 2026-05-25

## สิ่งที่ทำสำเร็จ ✅
- First post สำเร็จ (session ก่อนหน้า)
- เปลี่ยน Profile Pic สำเร็จผ่าน Graph API
- เปลี่ยน Cover Photo สำเร็จผ่าน Graph API
- รูปจาก Origin (Codex/DALL-E) — request ผ่าน Nexus relay
- สร้าง docs structure ครบ: guides, troubleshooting, platforms
- สร้าง credentials-tracker + credential mapping (page → account)
- สร้าง Playwright login tool + saved session

## สิ่งที่ผิดพลาด ❌
- ใช้เวลา 2+ ชม. ที่ควรจบใน 15 นาที
- ลอง Playwright automate FB UI ซ้ำ 5+ รอบ โดยไม่เปลี่ยนวิธี
- ไม่รู้ว่า mamipogo1@hotmail.com = PapaJinna (ไม่รู้ credentials ของงานตัวเอง)
- ไม่ถาม Nexus ตั้งแต่แรก (มี context จาก session ก่อน)
- install playwright ใหม่ทั้งที่มีอยู่แล้ว
- ไม่รู้ว่าต้องเปิด Standard Access ก่อนใช้ pages_manage_metadata
- ส่ง file upload ไปลง comment box แทน profile pic
- เดาสุ่ม coordinates click ใน FB UI ซ้ำๆ

## บทเรียนสำคัญ 📝
1. **ถาม Nexus/fleet ก่อน** ถ้าติดปัญหา — ไม่ต้องแก้เอง
2. **ลอง max 2 วิธี** → ถ้ายังไม่ได้ หยุดถาม Master J
3. **รู้ credentials ของตัวเอง** — SM Manager ต้องรู้ว่าเพจไหนใช้บัญชีอะไร
4. **เปิด Standard Access** ก่อนใช้ permission ใหม่ใน development mode
5. **อย่า automate FB UI** — ใช้ API ดีกว่า ถ้ามี permission ถูกต้อง
6. **Cover photo ส่ง ID ตรงๆ** — `cover=123456` ไม่ใช่ JSON
7. **สร้างเครื่องมือถาวร** — ไม่ใช่ throwaway script ใหม่ทุกครั้ง
8. **ค้นหา session history ก่อน** — ดู JSONL/retro ก่อนเขียนจากศูนย์

## วิธีที่ถูกต้อง (สำหรับครั้งหน้า)
```bash
# Profile pic — 1 command
curl -s -X POST "https://graph.facebook.com/v25.0/${PAGE_ID}/picture" \
  -F "source=@<file>" -F "access_token=$PAGE_TOKEN"

# Cover — 2 commands
PHOTO_ID=$(curl -s -X POST "https://graph.facebook.com/v25.0/${PAGE_ID}/photos" \
  -F "source=@<file>" -F "published=false" \
  -F "access_token=$PAGE_TOKEN" | python3 -c "import sys,json; print(json.load(sys.stdin)['id'])")
curl -s -X POST "https://graph.facebook.com/v25.0/${PAGE_ID}" \
  -d "cover=$PHOTO_ID" -d "access_token=$PAGE_TOKEN"
```

## ไฟล์ที่สร้าง/แก้ไข
| ไฟล์ | Action |
|------|--------|
| `docs/guides/facebook-page-complete-guide.md` | NEW — คู่มือฉบับสมบูรณ์ |
| `docs/guides/facebook-page-image-management.md` | NEW — คู่มือเปลี่ยนรูปเพจ |
| `docs/troubleshooting/facebook-page-images.md` | NEW — ปัญหา+วิธีแก้ |
| `docs/troubleshooting/playwright-setup.md` | NEW — Playwright setup |
| `docs/platforms/` | NEW — folder structure ทุก platform |
| `config/credentials-tracker.md` | UPDATED — เพิ่ม account mapping |
| `scripts/set-page-images.ts` | NEW — Playwright tool (ใช้ได้บางส่วน) |
| `.fb-session/state.json` | NEW — FB login session |

## รูปที่ใช้

### v1 (realistic — เปลี่ยนออกแล้ว)
- Profile: `ψ/inbox/from-origin/prism_profile_170x170.png`
- Cover: `ψ/inbox/from-origin/prism_cover_820x312.png`
- ปัญหา: Master J ว่าดูแข็งๆ เหมือน stock photo

### First Content Post ✅
- Post ID: `1184469221407318_122099317251337114`
- รูป: `ψ/inbox/from-origin/prism_first_post.png` (kawaii prism จดไดอารี่)
- หัวข้อ: วันแรกของ Prism Chronicle — บทเรียนเรื่องการเรียนรู้จากความผิดพลาด
- เหตุผล: Master J อนุมัติ — โพสแนะนำตัว + แชร์ประสบการณ์วันแรก
- อนุมัติโดย: Master J

### v2 (kawaii pastel — ใช้อยู่ปัจจุบัน) ✅
- Profile: `ψ/inbox/from-origin/prism_profile_v2.png` (resize 512x512 เพราะ FB ไม่รับ 170x170)
- Cover: `ψ/inbox/from-origin/prism_cover_v2.png`
- เหตุผลเปลี่ยน: Master J feedback ว่า v1 แข็งไป → ปรับเป็น minimal + น่ารัก + กลิ่น AI
- Caption: "v2: ปรับจาก realistic เป็น minimal kawaii pastel ตาม feedback Master J"
- Note: FB API profile pic ต้อง > 170x170 (ขั้นต่ำ ~200x200)
